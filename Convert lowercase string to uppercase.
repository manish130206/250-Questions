
#include <stdio.h>
#include<string.h>

int main() {
    char str[50];
    int n;
    printf("Enter String1 :");
    scanf("%s",&str);
   n=strlen(str);
   for(int i=0;i<n;i++){
       str[i]=str[i]-32;
   }
   printf("In Uppar Case : %s",str);
    return 0;
}
