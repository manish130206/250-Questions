
#include <stdio.h>
#include<string.h>

int main() {
    char str[50];
    printf("Enter String :");
    scanf("%s",&str);
    
  int len=strlen(str);
  printf("The Length of String is :%d",len);
    
    return 0;
}
