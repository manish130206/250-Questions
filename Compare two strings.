
#include <stdio.h>
#include<string.h>

int main() {
    char str[50],str1[50];
    int n;
    printf("Enter String1 :");
    scanf("%s",&str);
    printf("Enter String2 :");
    scanf("%s",&str1);
    
  n= strcmp(str,str1);
  if(n==0){
  printf("String is Equal");
  }
  else{
      printf("String is Not Equal");
  }
    return 0;
}
